# Sumocen

## 开发依赖
```
npm install --save express // 基石
npm install babel-preset-env --save-dev // babel
npm i babel-register --save-dev // babel
npm install -g nodemon // 开发模式下修改服务器自动重启

npm install babel-cli --save-dev // babel 开发打包转码
```
